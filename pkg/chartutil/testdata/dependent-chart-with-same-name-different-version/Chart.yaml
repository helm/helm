apiVersion: v2
name: parent-chart
version: 0.1.0
appVersion: v0.1.0
dependencies:
  - name: test
    version: "0.1.0"
    condition: one.enabled

  - name: test
    version: "0.2.0"
    condition: two.enabled