apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name | lower }}-{{ .Chart.Name | lower }}-{{ randAlpha 4 | lower }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
