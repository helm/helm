apiVersion: v1
kind: Service
metadata:
  name: {{ include "grandchild.fullname" . }}
  labels:
    {{- include "grandchild.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "grandchild.selectorLabels" . | nindent 4 }}
