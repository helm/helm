---
apiVersion: v1
name: fullenv
type: cli/v1
runtime: subprocess
config:
  shortHelp: "show env vars"
  longHelp: "show all env vars"
  ignoreFlags: false
runtimeConfig:
  platformCommand:
  - command: "$HELM_PLUGIN_DIR/fullenv.sh"
