apiVersion: v1
kind: Service
metadata:
  name: {{ include "bigchart.fullname" . }}
  labels:
    {{- include "bigchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bigchart.selectorLabels" . | nindent 4 }}
