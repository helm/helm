apiVersion: v1
kind: Pod
metadata:
  name: "{{.Release.Name}}-{{.Values.Name}}"
spec:
  containers:
  - name: {{ .Values.nonExistentKey | b64enc }}
    image: "alpine:3.9"
    command: ["/bin/sleep","9000"]
