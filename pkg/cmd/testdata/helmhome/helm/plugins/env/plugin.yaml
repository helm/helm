---
apiVersion: v1
name: env
type: cli/v1
runtime: subprocess
config:
  shortHelp: "env stuff"
  longHelp: "show the env"
  ignoreFlags: false
runtimeConfig:
  platformCommand:
  - command: "echo $HELM_PLUGIN_NAME"
