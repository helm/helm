# Default values for parent-chart.
nameOverride: parent-chart
one:
  enabled: false
two:
  enabled: true
resources:
  autoscaler:
    minReplicas: 0
    maxReplicas: 0
    targetCPUUtilizationPercentage: 99