apiVersion: v1
kind: Service
metadata:
  name: {{ include "otlp_forwarder.fullname" . }}
  labels:
    {{- include "otlp_forwarder.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "otlp_forwarder.selectorLabels" . | nindent 4 }}
