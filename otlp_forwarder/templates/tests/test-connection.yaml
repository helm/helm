apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "otlp_forwarder.fullname" . }}-test-connection"
  labels:
    {{- include "otlp_forwarder.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "otlp_forwarder.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
