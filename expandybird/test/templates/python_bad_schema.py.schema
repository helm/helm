info:
  title: Schema with several errors

imports:

properties:
  bad-type:
    type: int
  missing-cond:
    type: string
    exclusiveMaximum: 10
  odd-string:
    type: string
    not:
      multipleOf: 2
  bad-enum:
    type: string
    enum: not a list

