apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart-missing-version.fullname" . }}
  labels:
    {{- include "chart-missing-version.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chart-missing-version.selectorLabels" . | nindent 4 }}
