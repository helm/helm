apiVersion: v1
kind: Service
metadata:
  name: {{ include "issue-5550.fullname" . }}
  labels:
    {{- include "issue-5550.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "issue-5550.selectorLabels" . | nindent 4 }}
