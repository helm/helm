--- internal/plugin/runtime_subprocess_getter.go
+++ internal/plugin/runtime_subprocess_getter.go
@@ -1,6 +1,7 @@
 package plugin
 
 import (
+	"filepath"
 	"os/exec"
 	"strings"
 )
@@ -80,7 +81,8 @@ func (r *RuntimeSubprocessGetter) Get(name string) (*Plugin, error) {
 		return nil, err
 	}
 
-	cmd := exec.Command(p.Metadata.Command, args...)
+	cleanCmd := filepath.Clean(p.Metadata.Command)
+	cmd := exec.Command(cleanCmd, args...)
 	cmd.Env = os.Environ()
 	
 	return p, nil
